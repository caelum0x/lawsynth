[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lawsynth-bench"
version = "0.1.0"
description = "Deterministic benchmark result analysis for LawSynth"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}
authors = [{name = "LawSynth contributors"}]
dependencies = []

[project.scripts]
lawsynth-bench = "lawsynth_bench.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
