# Trajectory accuracy

Use aligned numerical reference and prediction sequences with `trajectory_accuracy.score`. It returns MAE and RMSE and rejects unequal or empty sequences through the underlying metric validation.
