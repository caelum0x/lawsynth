# Failure analysis

Use `error_rate` with recorded Boolean success outcomes. `classify` counts stable prefixes before a colon in error messages, retaining the evidence rather than guessing root causes.
