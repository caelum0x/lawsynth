# Equation recovery

Equation scoring normalizes whitespace and top-level additive-term order, then reports exact equality and set-based term precision, recall, and F1. It is not a computer-algebra equivalence prover; semantic transformations must be normalized by the producing harness.
