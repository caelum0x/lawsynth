# Uncertainty coverage

`coverage` computes the fraction of recorded observations inside closed lower/upper intervals. Invalid intervals and mismatched arrays are rejected. It does not calibrate or construct uncertainty intervals.
