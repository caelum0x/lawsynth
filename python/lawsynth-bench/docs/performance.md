# Performance comparison

Only recorded time-unit observations (`ns`, `us`, `ms`, `s`) are selected. Comparisons require identical metric groups. A regression is thresholded by `BenchmarkConfig`; this is a deterministic policy check, not a statistical significance claim.
