# Errors

`ArtifactValidationError` reports malformed decoded input. `UnsupportedCapabilityError`
reports deliberately absent functionality such as remote service connections.
Both inherit `NotebookError`.
