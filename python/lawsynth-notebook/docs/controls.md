# Controls

`RangeControl` describes a bounded scalar input and validates finite bounds,
value, and positive step. It is declarative metadata; changing it does not run
a simulation or mutate a world.
