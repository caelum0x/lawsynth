# Extension metadata

`extension_spec()` exposes declarative compatibility metadata for a separate
JupyterLab extension distribution. This Python package neither installs nor
pretends to provide that frontend extension.
