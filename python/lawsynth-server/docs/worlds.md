# Worlds

World records require a non-empty list of equation strings. This is schema
validation only: bounded expression parsing and native execution remain the
responsibility of the LawSynth core, and no Python evaluation of equation text
is performed by the server.
