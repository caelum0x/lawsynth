# Telemetry

Telemetry is disabled by default. When enabled, the built-in collector holds
only counters grouped by operation and status; it retains no request body,
token, dataset value, artifact content, or organization identifier.
