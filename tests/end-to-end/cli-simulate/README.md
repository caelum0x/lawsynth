# CLI simulation

Builds a checksum-protected `lawsynth-world` archive and exercises the compiled
`lawsynth simulate` command. The asserted trajectory is produced by the Rust
runtime, not by this fixture writer.
