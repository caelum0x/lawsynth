# Local Python library

Validates the pure Python SDK data boundary unconditionally. If its compiled
extension is installed, it creates, simulates, saves, and CLI-inspects a real
native world; otherwise the tested native-unavailable error is the supported
development boundary.
