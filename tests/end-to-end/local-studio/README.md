# Local Studio boundary

The current production workspace ships the CLI and SDK, not a local Studio
server. This executable case verifies the command is rejected rather than
pretending that a UI workflow exists.
