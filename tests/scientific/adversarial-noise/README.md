# Adversarial-noise case

An alternating ±0.01 perturbation is injected into a deterministic growth
series. The runner uses the real Savitzky–Golay discovery option and verifies
that it returns a finite executable candidate. This is deliberately a pipeline
stability check, not a guarantee of exact recovery under arbitrary noise.
