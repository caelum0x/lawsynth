# SIR candidate-law case

The deterministic susceptible–infected–recovered fixture has bilinear incidence
and linear recovery. It validates that the current polynomial discovery path
and simulator operate on a three-state observation set. It deliberately does
not interpret observational curve fitting as epidemic causality or intervention
effect estimation.
