# Lorenz candidate-law case

The fixture is generated from the Lorenz equations with standard parameters.
The runner calls the native discovery CLI and then simulates the discovered
three-state world over a short horizon. Chaotic systems make long-run pointwise
forecast matching scientifically inappropriate, so this is explicitly an
executable-candidate and finite-output test, not a trajectory-accuracy claim.
