# Regime-recovery boundary

The fixture switches an exponential growth rate half way through the series.
The runner invokes native discovery and checks that it produces the current
continuous-world representation. It does not call this a recovered regime:
change-point and regime inference are beyond the P1/P2 CLI contract.
