# Uncertainty-coverage boundary

The case requests the native bootstrap path on deterministic growth data and
requires a valid candidate archive. It does not manufacture a confidence band
or a coverage score: the P1/P2 CLI does not return propagated trajectory
intervals, so claiming empirical coverage would be scientifically false.
