# Python to Rust

When the extension is installed, a Python `World` serializes an actual bundle
which the Rust CLI loads. Without the extension, the public SDK's precise
native-unavailable boundary remains verified.
