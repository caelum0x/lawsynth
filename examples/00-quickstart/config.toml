name = "Quickstart logistic growth"
kind = "quickstart"
states = ["population"]
initial = [1]
start = 0
stop = 10
step = 0.05
threshold = 0.0001
description = "Logistic growth: d(population)/dt = r population (1 - population / capacity)."

[parameters]
r = 0.8
capacity = 10
