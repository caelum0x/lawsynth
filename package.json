{
  "name": "lawsynth-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace tooling for the implemented LawSynth TypeScript packages.",
  "license": "Apache-2.0",
  "engines": { "node": ">=22" },
  "packageManager": "pnpm@10.18.2",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "check:workspace": "node --input-type=module -e \"import fs from 'node:fs'; JSON.parse(fs.readFileSync('package.json')); console.log('workspace metadata valid')\""
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
