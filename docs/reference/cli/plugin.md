# Plugins

The initial offline CLI has no plugin discovery, loading, sandboxing, or plugin command. A command named `plugin` is not accepted. This is an intentional capability boundary: bundles and equations are decoded only by the compiled LawSynth implementation.
