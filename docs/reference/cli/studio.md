# Studio boundary

There is no Studio GUI, browser application, or `lawsynth studio` subcommand in the repository's executable distribution. Use the CLI, Rust crates, or Python extension for supported offline work.
