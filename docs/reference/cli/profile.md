# Profiling

The library can produce a compact dataset profile, but the executable currently does not expose a `profile` subcommand. There is no CLI report format, profile file, or automatic preprocessing selection to rely on. Use discovery with explicitly selected options instead.
