# Internal scaffolding

These documents are historical planning and generation artifacts. They are kept
for reference but are **not part of the published v0.1 surface** and are not
maintained as user-facing docs.

- `LawSynth_3161_Repository_Manifest.md` — the original generated repository
  manifest (file-by-file plan for P0–P5).
- `LawSynth_Production_Architecture.md` — the original production architecture
  blueprint.

For the current, maintained docs see the top-level `README.md`,
`ARCHITECTURE.md`, and `docs/getting-started/`.
