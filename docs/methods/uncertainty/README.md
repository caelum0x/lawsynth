# Uncertainty utilities

`lawsynth-uncertainty` provides checked scalar samples, deterministic empirical bootstrap, percentile intervals, covariance estimation, first-order propagation, independent-input Monte Carlo propagation, local quadratic profile summaries, and explicit structural uncertainty aggregation.

Each routine quantifies the inputs and assumptions it receives. The crate does not infer a universal uncertainty model for a discovered law.
