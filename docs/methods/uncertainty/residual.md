# Residual uncertainty boundary

The uncertainty crate does not fit a residual distribution or convert model residuals into predictive intervals. The score crate can compute residual summaries, but no coupling is made automatically.

Residual bootstrap, heteroscedastic likelihoods, autocorrelation models, and observation-noise estimation require a separate explicit procedure.
