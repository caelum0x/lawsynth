# Pareto frontier

`pareto_by_loss_and_complexity` retains candidates not dominated in both supplied scalar loss and integer complexity, preserving input order. It does not modify the loss or recompute complexity.

Pareto membership is not a probability, a uniqueness guarantee, or a scientific validation result. Multiple structurally distinct expressions can remain tied.
