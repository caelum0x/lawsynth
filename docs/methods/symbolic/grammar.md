# Grammar

`Grammar::scalar` accepts identifiers, sorts and deduplicates them, and defines scalar terminals. Enumeration also includes the constant one, then combines frontier expressions by addition and, when enabled, multiplication.

There are no typed non-scalar productions, user-defined operators, transcendental productions, dimensional grammar rules, or grammar-learning mechanism in this component.
