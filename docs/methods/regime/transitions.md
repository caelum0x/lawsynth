# Transition accounting

The transition utilities derive a transition-count matrix from an observed sequence of regime indices and normalize rows where possible. This is descriptive accounting of a supplied path.

It neither estimates latent states nor supplies confidence intervals for transition probabilities. State labels are opaque indices and have no cross-run semantic alignment.
