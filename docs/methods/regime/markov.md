# Markov-model boundary

The HMM component accepts a fully specified discrete Markov transition matrix and emission table; it does not learn them. The BOCPD component is not an HMM and represents run lengths under a constant hazard.

Continuous-time Markov switching, semi-Markov dwell-time models, and switching differential-equation inference are not implemented.
