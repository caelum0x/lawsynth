# Communicating uncertainty

Report the target quantity, data source, estimator, assumptions, interval construction, resampling count, seed, and numerical solver settings. For model uncertainty, list the candidates considered and the criterion used to retain or reject them.

Avoid describing a standard error, percentile interval, or sensitivity quantity as a universal confidence statement. The library returns calculations; study design determines what those calculations support.
