# Uncertainty

`lawsynth-uncertainty` offers dependency-free in-memory primitives: validated samples, empirical bootstrap draws, percentile confidence intervals, covariance estimation, linear propagation, independent empirical Monte Carlo, local quadratic profiles, and structural uncertainty aggregation.

Each calculation exposes its input assumptions. Preserve the resampling configuration, seed, model evaluation function, and source data when you publish an interval or propagated result.
