# Counterfactual scenarios

Build a `Scenario` with initial state, parameter overrides, control inputs, and ordered interventions, then simulate the same native world under each scenario. Compare trajectories at matching times and retain each scenario definition with the result.

This workflow provides deterministic model counterfactuals. It does not abduct latent exogenous variables from observed data or compute individual-level structural causal-model counterfactuals.
