# Visualization data

Use `Segmentation::segments`, `change_points`, `BocpdPoint`, or `ViterbiPath.states` as direct series for plots. Plot source observations, boundaries, and configuration together so a reviewer can see the criterion behind each partition.

LawSynth returns structured numerical results only. It does not ship a chart renderer, dashboard, confidence band calculation for segments, or automated annotation policy.
