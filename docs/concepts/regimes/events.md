# Regime events

Change-point indices identify boundaries between observations. Convert an index to an external timestamp through the source time axis before you report an event. Preserve the segmentation configuration and the mapping used for that conversion.

World events are separate utilities: they record known zero-crossing information and split intervals. A detected statistical change point does not automatically trigger a World intervention or state reset.
