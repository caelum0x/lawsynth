# Shared laws across segments

You can discover or fit a law on a selected segment and compare its candidate metrics across segments. LawSynth does not yet fit a shared parameterized World jointly over many segments or test parameter equality between them.

Use identical dataset preprocessing, derivative configuration, feature library, sparse configuration, and evaluation probes if you compare separately fitted segment models. Differences can otherwise come from the pipeline rather than the regime.
