# LawSynth's implemented local engine requires no environment variables.
# Keep credentials and machine-specific paths in an untracked .env file if
# later tooling needs them; never add them to this example.
