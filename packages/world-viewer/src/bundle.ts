import { createWorldViewModel, type ViewerWorld, type WorldViewModel } from "./viewer.js";
export interface BundleEntry { readonly path: string; readonly mediaType: string; readonly sha256: string; readonly bytes: number; }
export interface ViewerBundle { readonly format: "lawsynth-world"; readonly format_version: string; readonly world_encoding: string; readonly world: ViewerWorld; readonly entries?: readonly BundleEntry[]; }
export function parseViewerBundle(input: string | unknown): ViewerBundle { const value: unknown = typeof input === "string" ? JSON.parse(input) : input; if (!value || typeof value !== "object") throw new TypeError("bundle must be an object"); const bundle = value as Partial<ViewerBundle>; if (bundle.format !== "lawsynth-world" || typeof bundle.format_version !== "string" || typeof bundle.world_encoding !== "string" || !bundle.world) throw new TypeError("bundle is missing required LawSynth world metadata"); for (const entry of bundle.entries ?? []) if (!entry.path || !entry.mediaType || !/^[0-9a-f]{64}$/iu.test(entry.sha256) || !Number.isSafeInteger(entry.bytes) || entry.bytes < 0) throw new TypeError(`invalid bundle entry ${entry.path}`); return bundle as ViewerBundle; }
export function viewBundle(input: string | unknown): WorldViewModel { return createWorldViewModel(parseViewerBundle(input).world); }
