import { assertRustWorldSource, parseRustUnit, validateBundleCatalog, validateRustWorldSource } from "../src/validators.js";
import { equal, ok, throws } from "./test-support.js";
const source = { formatVersion: "0.1.0", id: "decay", time: { kind: "continuous" as const, unit: "s" }, variables: [{ id: "x", role: "state" as const, unit: "1" }], parameters: [{ id: "rate", value: 0.2, unit: "1/s" }], laws: [{ kind: "continuous" as const, target: "x", expression: { kind: "binary" as const, operator: "mul" as const, left: { kind: "unary" as const, operator: "neg" as const, operand: { kind: "symbol" as const, id: "rate" } }, right: { kind: "symbol" as const, id: "x" } } }] };
export function runValidatorTests(): void { ok(validateRustWorldSource(source).ok); equal(validateRustWorldSource({ ...source, laws: [{ ...source.laws[0], kind: "stochastic" }] }).ok, false); equal(parseRustUnit("kg*m/s^2")?.[2], -2); equal(parseRustUnit("N"), undefined); throws(() => assertRustWorldSource({})); ok(validateBundleCatalog({ worldId: "w", createdAt: "2026-08-16T00:00:00Z", root: "world/world.bin", entries: [{ path: "world/world.bin", mediaType: "application/octet-stream", sha256: "0".repeat(64), bytes: 0 }] }).ok); }
