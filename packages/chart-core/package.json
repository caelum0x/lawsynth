{
  "name": "@lawsynth/chart-core",
  "version": "0.1.0",
  "description": "Dependency-free, deterministic chart data transformations for LawSynth trajectories.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "license": "Apache-2.0",
  "sideEffects": false
}
