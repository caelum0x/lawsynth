[package]
name = "lawsynth-artifact-service"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Local-first, content-addressed artifact lifecycle service for LawSynth."

[dependencies]
lawsynth-bundle = { path = "../../crates/lawsynth-bundle", version = "0.1.0" }
lawsynth-store = { path = "../../crates/lawsynth-store", version = "0.1.0" }

[lib]
path = "src/lib.rs"

[[bin]]
name = "lawsynth-artifact"
path = "src/main.rs"
