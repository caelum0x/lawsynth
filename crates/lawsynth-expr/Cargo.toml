[package]
name = "lawsynth-expr"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Deterministic scalar expression IR and evaluator for LawSynth."
autoexamples = false

[dependencies]
lawsynth-core = { path = "../lawsynth-core" }

[lib]
path = "src/lib.rs"
