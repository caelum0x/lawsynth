[package]
name = "lawsynth-modelreduce"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Deterministic linear model-order reduction (balanced truncation) for discovered LawSynth models."

[dependencies]
lawsynth-core = { path = "../lawsynth-core" }
lawsynth-koopman = { path = "../lawsynth-koopman" }

[lib]
path = "src/lib.rs"
