[package]
name = "lawsynth-mpc"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Deterministic receding-horizon (model-predictive) control for discovered LawSynth models."

[dependencies]
lawsynth-core = { path = "../lawsynth-core", version = "0.1.0" }
lawsynth-expr = { path = "../lawsynth-expr", version = "0.1.0" }
lawsynth-jacobian = { path = "../lawsynth-jacobian", version = "0.1.0" }
lawsynth-feedback = { path = "../lawsynth-feedback", version = "0.1.0" }
lawsynth-koopman = { path = "../lawsynth-koopman", version = "0.1.0" }

[lib]
path = "src/lib.rs"
