[package]
name = "lawsynth-jacobian"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Deterministic analytic Jacobian codegen from LawSynth expression trees."

[dependencies]
lawsynth-core = { path = "../lawsynth-core", version = "0.1.0" }
lawsynth-expr = { path = "../lawsynth-expr", version = "0.1.0" }

[dev-dependencies]
# Used only in tests to cross-check the symbolic Jacobian against numerical
# finite differences of the field.
lawsynth-differentiate = { path = "../lawsynth-differentiate", version = "0.1.0" }

[lib]
path = "src/lib.rs"
