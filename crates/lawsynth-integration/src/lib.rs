//! Cross-crate integration tests for LawSynth (no public API).
//!
//! This crate deliberately exposes nothing. It exists only to host end-to-end
//! `tests/` that drive many LawSynth crates together — discovery through
//! analysis and control — to prove the pieces compose and stay consistent.
