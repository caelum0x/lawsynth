[package]
name = "lawsynth-weakform"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Weak / integral-form discovery for noise-robust LawSynth system identification."

[dependencies]
lawsynth-core = { path = "../lawsynth-core", version = "0.1.0" }
lawsynth-data = { path = "../lawsynth-data", version = "0.1.0" }
lawsynth-features = { path = "../lawsynth-features", version = "0.1.0" }

[lib]
path = "src/lib.rs"
