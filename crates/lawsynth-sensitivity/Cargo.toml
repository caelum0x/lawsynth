[package]
name = "lawsynth-sensitivity"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Deterministic forward sensitivity analysis of discovered LawSynth models via the variational equations."

[dependencies]
lawsynth-core = { path = "../lawsynth-core", version = "0.1.0" }
lawsynth-expr = { path = "../lawsynth-expr", version = "0.1.0" }
lawsynth-jacobian = { path = "../lawsynth-jacobian", version = "0.1.0" }

[lib]
path = "src/lib.rs"
