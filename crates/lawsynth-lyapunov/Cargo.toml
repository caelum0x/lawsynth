[package]
name = "lawsynth-lyapunov"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Deterministic Lyapunov-exponent estimation (chaos diagnostic) for discovered LawSynth models."

[dependencies]
lawsynth-core = { path = "../lawsynth-core" }
lawsynth-expr = { path = "../lawsynth-expr" }
lawsynth-jacobian = { path = "../lawsynth-jacobian" }

[lib]
path = "src/lib.rs"
