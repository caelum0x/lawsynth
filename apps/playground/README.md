# LawSynth Playground

The Playground is a client-only World editor and bounded local simulation host.
It accepts generated JavaScript bindings for `lawsynth-wasm`; it does not claim
that the Rust crate itself exposes browser bindings. World source, datasets,
parameter overrides, simulations, local saves, and share links are validated and
size-limited at their browser boundaries.
