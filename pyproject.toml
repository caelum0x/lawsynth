[project]
name = "lawsynth-workspace"
version = "0.1.0"
description = "Workspace tooling metadata for LawSynth."
requires-python = ">=3.11"
license = { text = "Apache-2.0" }

[tool.uv]
package = false
