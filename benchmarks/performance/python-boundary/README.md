# Python SDK boundary

This measures real validation and immutable construction through public `lawsynth` Python SDK types. It deliberately does not claim native-extension discovery throughput, which depends on an installed extension. Run `python run.py --workdir /tmp/lawsynth-python`.
