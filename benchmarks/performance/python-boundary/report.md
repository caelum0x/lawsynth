# Reporting

The workload validates 2,000 aligned finite observations and discovery configuration through the SDK API. Record the result with the Python version and platform; there is no synthetic pass time.
