# Simulation workflow

This measures discovery, persisted-world inspection, and native simulation as one public workflow. It uses the generated exponential-decay series, not a prebuilt fixture. Run `python run.py --workdir /tmp/lawsynth-simulation`.
