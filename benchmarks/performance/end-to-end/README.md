# End-to-end workflow

This is an executable public workflow: generate observations, discover a world, inspect its persisted bundle, and simulate it. It measures no mocked components. Run `python run.py --workdir /tmp/lawsynth-e2e`.
