# Bundle I/O

This measures actual bundle creation by discovery and subsequent `lawsynth inspect` parsing. The generated dataset and bundle live in the supplied work directory. Run `python run.py --workdir /tmp/lawsynth-bundle-io`.
