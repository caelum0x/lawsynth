# Reporting

Compare `elapsed_ns` only between runs on the same machine and revision. Correctness is gated by the native discovery and inspection return codes; no absolute timing threshold is encoded in source control.
