# Native discovery expression evaluation

This benchmark measures the public `lawsynth discover` workflow with a trigonometric library enabled. It records an elapsed monotonic duration only after creation and inspection of a real `.lsworld` bundle succeed. Run `python run.py --workdir /tmp/lawsynth-expression`.
