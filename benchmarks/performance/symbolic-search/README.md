# Library search

This benchmark exercises the native discovery library with trigonometric features enabled; it does not claim a separate public symbolic-search API. Run `python run.py --workdir /tmp/lawsynth-library-search`.
